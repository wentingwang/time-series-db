# test simple query with mixed wildcard and literal tag filter
# test tag filters split over multiple lines, delimited by only a new line
fetch
name:service.errors
region:{us-*,ca}
| transformNull
| moving 1m sum
| sum region
| average
| minSeries region
| maximum
| keepLastValue
| >= 5
| count
| alias {{.region}}
