DIFF(keepNans=false,groupBy=[])
  DIVIDE_SERIES(groupBy=[name, region])
    AS_PERCENT(groupBy=[service])
      DIFF(keepNans=true,groupBy=[])
        FETCH({name=[a]}, !{})
        FETCH({name=[b]}, !{})
      DIFF(keepNans=false,groupBy=[])
        FETCH({name=[c]}, !{})
        FETCH({name=[b]}, !{})
    DIFF(keepNans=true,groupBy=[name])
      FETCH({name=[e]}, !{})
      FETCH({name=[f]}, !{})
  DIFF(keepNans=false,groupBy=[name, city])
    FETCH({name=[g]}, !{})
    FETCH({name=[h]}, !{})
